global:
  resolve_timeout: 5m

route:
  receiver: pilot-ops
  group_by: ["alertname", "school", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: pilot-ops
    webhook_configs:
      - url: http://localhost:9000/alerts
        send_resolved: true
